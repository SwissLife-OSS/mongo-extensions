<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)..\'))" />

  <Import Project="$(MSBuildThisFileDirectory)TestProject.props"
          Condition="$(MSBuildProjectName.Contains('Tests'))" />
</Project>
